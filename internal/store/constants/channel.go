package constants

const (
	// BroadcastChannel defines the name of the broadcast channel used for inter-service communication.
	BroadcastChannel = "broadcast"

	// LockChannel defines the name of the lock channel used for distributed locking mechanisms.
	LockChannel = "lock"

	// StateChannel defines the name of the state channel used for state management and updates.
	StateChannel = "state"
)
